<%- include('../partials/header') %>
  <% for (let i = 0; i < skill.level; i++) { %>
    <i class="bi bi-circle-fill green"></i>
  <% } %>
  <% for (let i = 0; i < 10 - skill.level; i++) { %>
    <i class="bi bi-circle grey"></i>
  <% } %>
  <h3><%= skill.skill %></h3>
  <p> <%= skill.description %> </p>
<%- include('../partials/footer') %>